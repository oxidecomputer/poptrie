[workspace]
members = [".", "test-util"]

[package]
name = "poptrie"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[dev-dependencies]
criterion = "0.5"
proptest = "1"
poptrie-test-util = { path = "test-util", features = ["proptest"] }

[[bench]]
name = "lookup"
harness = false
